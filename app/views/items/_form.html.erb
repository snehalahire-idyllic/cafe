<%= form_for([@menu , @menu.items.build]) do |f|%> 
  <%= f.text_field :name,placeholder: "New item" %>
  <%= f.text_field :rate,placeholder: "Rate" %>
  <%= f.text_field :quantity,placeholder: "Quantity" %>

  <%= f.submit %>

<% end %>